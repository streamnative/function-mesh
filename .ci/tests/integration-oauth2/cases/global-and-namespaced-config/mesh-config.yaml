apiVersion: compute.functionmesh.io/v1alpha1
kind: BackendConfig
metadata:
  name: backend-config
  namespace: default
spec:
  autoUpdate: true
  pod:
    liveness:
      initialDelaySeconds: 30
      periodSeconds: 10
